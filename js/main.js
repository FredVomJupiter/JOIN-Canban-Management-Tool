/*function fadeInLoginPage() {
    setTimeout(() => {
      let loginContainer = document.getElementById("loginContainer");
      let opacity = 0;
      let fadeInterval = setInterval(() => {
        if (opacity >= 1) {
          clearInterval(fadeInterval);
        } else {
          opacity += 0.1;
          loginContainer.style.opacity = opacity;
        }
      }, 50);
    }, 1000);
  }
  fadeInLoginPage();*/